---
const { name = 'Button', href = '', className = '' } = Astro.props;
---

{href ? (
  <a
    href={href}
    target="_blank"
    rel="noopener noreferrer"
    class={`bg-white hover:bg-gray-100 text-black font-semibold py-3 px-4 rounded-md transition-colors duration-200 inline-block ${className}`}
  >
    {name}
  </a>
) : (
  <button
    class={`bg-white hover:bg-gray-100 text-black font-semibold py-3 px-4 rounded-md transition-colors duration-200 ${className}`}
  >
    {name}
  </a>
)}